// Next.js configuration file - defines build and runtime settings for the application
// Configures image domains, webpack settings, and other Next.js specific options
/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['images.unsplash.com'],
  },
}

module.exports = nextConfig